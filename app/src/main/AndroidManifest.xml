<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Разрешения -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Запрос пакетов для Ace Stream -->
    <queries>
        <package android:name="org.acestream.engine" />
        <package android:name="org.acestream.media" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="acestream" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AcePlayer"
        tools:targetApi="31">

        <!-- ✅ AuthActivity - стартовый экран (с intent-filter) -->
        <activity
            android:name=".ui.auth.AuthActivity"
            android:exported="true"
            android:theme="@style/Theme.AcePlayer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ MainActivity - ОБЯЗАТЕЛЬНО объявить! -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AcePlayer">

            <!-- Обработка acestream:// ссылок -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="acestream" />
            </intent-filter>

        </activity>

        <!-- ✅ LinksActivity (если используется) -->
        <activity
            android:name=".LinksActivity"
            android:label="@string/title_links"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.AcePlayer" />

    </application>
</manifest>